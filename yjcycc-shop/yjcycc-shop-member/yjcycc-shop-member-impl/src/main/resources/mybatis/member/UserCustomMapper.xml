<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yjcycc.shop.member.mapper.UserCustomMapper">
	
	<!-- table name -->
	<sql id="tableName">
		yjcycc_user_custom
	</sql>
	
	<!-- select field -->
	<sql id="selectField">
		id,
		mobile,
		password,
		pay_password as payPassword,
		balance,
		nick,
		insert_time as insertTime,
		update_time as updateTime
	</sql>
	
	<!-- add field -->
	<sql id="addField">
		mobile,
		password,
		pay_password,
		balance,
		nick,
		insert_time,
		update_time
	</sql>
	
	<!-- field values -->
	<sql id="FieldValue">
		#{mobile},
		#{password},
		#{payPassword},
		#{balance},
		#{nick},
		#{insertTime},
		#{updateTime}
	</sql>
	
	<!-- save -->
	<insert id="save" parameterType="userCustom">
		insert into 
		<include refid="tableName"></include>
		(
		<include refid="addField"></include>
		) values (
		<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- findById -->
	<select id="findById" resultType="userCustom" parameterType="String">
		select 
		<include refid="selectField"></include>
		from 
		<include refid="tableName"></include>
		where 
			id = #{id}
	</select>
	
	<!-- 分页, 使用了pagehelper插件 -->
	<select id="findPager" resultType="userCustom" parameterType="map">
		select 
		<include refid="selectField"></include>
		from 
		<include refid="tableName"></include>
        where 1 = 1
        	and mobile = #{mobile}
		order by insert_time desc
	</select>
</mapper>