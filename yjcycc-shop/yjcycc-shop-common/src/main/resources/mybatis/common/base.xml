<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yjcycc.shop.common.mapper.BaseMapper">
	
	<!-- table name -->
	<sql id="tableName">
		
	</sql>
	
	<!-- select field -->
	<sql id="selectField">
		
	</sql>
	
	<!-- add field -->
	<sql id="addField">
		
	</sql>
	
	<!-- field values -->
	<sql id="FieldValue">
		
	</sql>
	
	<!-- save -->
	<insert id="save" parameterType="order">
		insert into 
			<include refid="tableName"></include>
		(
			<include refid="addField"></include>
		) values (
			<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- findById -->
	<select id="findById" resultType="order" parameterType="String">
		select 
			<include refid="selectField"></include>
		from 
			<include refid="tableName"></include>
		where 
			id = #{id}
	</select>
	
	<!-- findPager -->
	<select id="findPager" resultType="order" parameterType="map">
		select 
			<include refid="selectField"></include>
		from 
			<include refid="tableName"></include>
        where 1 = 1
		order by insert_time desc
	</select>
</mapper>